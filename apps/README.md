# Apps (widgets)

Each widget lives in its own folder under `apps/`.

Rules:
- Keep each widget deployable on its own.
- Prefer shared code from `packages/` instead of duplication.
- Each widget must document: purpose, inputs, how to run, and how to deploy.
