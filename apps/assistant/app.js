const WORKER='https://widget-sync.lordgrape-widgets.workers.dev';const SESSION_KEY='command-centre-access-v1';const THEME_KEY='command-centre-theme-v1';const RUNTIME_KEY='command-centre-runtime-v12';const TODO_URL='https://notion-widgets-93r.pages.dev/todo-smart-shell.html?v=20260907-bidirectional-v1';const TIMETABLE_URL='https://lordgrape.github.io/notion-widgets/timetable-shell.html?v=20260907-command-bidirectional-v1&mode=command';const $=s=>document.querySelector(s);let accessKey='';let theme=localStorage.getItem(THEME_KEY)||(matchMedia('(prefers-color-scheme:light)').matches?'light':'dark'),syncResetTimer=null;async function repairRuntime(){try{if(localStorage.getItem(RUNTIME_KEY)==='ready')return;if('serviceWorker'in navigator){const registrations=await navigator.serviceWorker.getRegistrations();await Promise.all(registrations.map(registration=>registration.unregister()))}if('caches'in window){const keys=await caches.keys();await Promise.all(keys.filter(key=>key.startsWith('command-centre-')).map(key=>caches.delete(key)))}localStorage.setItem(RUNTIME_KEY,'ready')}catch(error){}}function applyTheme(next,reload=true){theme=next==='light'?'light':'dark';document.documentElement.dataset.theme=theme;localStorage.setItem(THEME_KEY,theme);const toggle=$('#themeToggle'),label=$('#themeLabel'),icon=$('#themeIcon');if(toggle){const target=theme==='dark'?'light':'dark';toggle.title=`Switch to ${target} mode · Ctrl Alt L`;toggle.setAttribute('aria-label',`Switch to ${target} mode`);toggle.setAttribute('aria-pressed',theme==='light'?'true':'false')}if(label)label.textContent=theme==='dark'?'Light':'Dark';if(icon)icon.textContent=theme==='dark'?'☀':'☾';const meta=document.querySelector('meta[name="theme-color"]');if(meta)meta.content=theme==='dark'?'#15111b':'#f5f2f8';if(reload&&accessKey)loadWidgets()}function toggleTheme(){applyTheme(theme==='dark'?'light':'dark')}function syncMessage(message){const node=$('#syncState');if(!node)return;const dot=node.querySelector('i');node.textContent=message;if(dot)node.prepend(dot)}function resetSyncMessage(){clearTimeout(syncResetTimer);syncResetTimer=setTimeout(()=>syncMessage('Private and synchronized'),4000)}function widgetMessage(event){const data=event.data||{};if(event.origin==='https://notion-widgets-93r.pages.dev'&&data.type==='command-centre:timetable-updated'){syncMessage('Syncing timetable…');const frame=$('#timetableFrame');if(frame&&frame.contentWindow)frame.contentWindow.postMessage({type:'command-centre:refresh-timetable'},'https://lordgrape.github.io');resetSyncMessage()}else if(event.origin==='https://lordgrape.github.io'&&data.type==='command-centre:todo-updated'){syncMessage('Syncing To-Do…');const frame=$('#todoFrame');if(frame&&frame.contentWindow)frame.contentWindow.postMessage({type:'command-centre:refresh-todo'},'https://notion-widgets-93r.pages.dev');resetSyncMessage()}else if(event.origin==='https://lordgrape.github.io'&&data.type==='command-centre:timetable-synced'){clearTimeout(syncResetTimer);syncMessage('Private and synchronized')}}async function authorize(value){const key=String(value||'').trim();if(!key)throw new Error('unauthorized');const response=await fetch(`${WORKER}/state/user`,{headers:{'X-Widget-Key':key},cache:'no-store'});if(response.status===401)throw new Error('unauthorized');if(!response.ok)throw new Error('unavailable');accessKey=key;sessionStorage.setItem(SESSION_KEY,key)}function widgetUrl(base){const url=new URL(base);url.searchParams.set('theme',theme);return`${url.toString()}#key=${encodeURIComponent(accessKey)}`}function loadWidgets(){const todo=widgetUrl(TODO_URL),timetable=widgetUrl(TIMETABLE_URL);$('#todoFrame').src=todo;$('#todoOpen').href=todo;$('#timetableFrame').src=timetable;$('#timetableOpen').href=timetable}function unlock(){$('#lockScreen').hidden=true;$('#appShell').setAttribute('aria-hidden','false');document.body.classList.remove('locked');loadWidgets()}function lock(){accessKey='';sessionStorage.removeItem(SESSION_KEY);$('#todoFrame').src='about:blank';$('#timetableFrame').src='about:blank';$('#accessKey').value='';$('#lockScreen').hidden=false;$('#appShell').setAttribute('aria-hidden','true');document.body.classList.add('locked');setTimeout(()=>$('#accessKey').focus(),0)}async function submit(e){e.preventDefault();const status=$('#unlockStatus');status.classList.remove('error');status.textContent='Verifying private access…';try{await authorize($('#accessKey').value);unlock()}catch(error){status.classList.add('error');status.textContent=error.message==='unauthorized'?'That key was not accepted.':'Private sync is temporarily unavailable.'}}async function init(){await repairRuntime();applyTheme(theme,false);$('#unlockForm').addEventListener('submit',submit);$('#lockButton').addEventListener('click',lock);$('#themeToggle').addEventListener('click',toggleTheme);window.addEventListener('message',widgetMessage);document.addEventListener('keydown',e=>{if(e.ctrlKey&&e.altKey&&e.key.toLowerCase()==='l'){e.preventDefault();toggleTheme()}});const saved=sessionStorage.getItem(SESSION_KEY);if(saved)try{await authorize(saved);unlock()}catch(error){lock()}else lock();if('serviceWorker'in navigator&&location.protocol.startsWith('http'))navigator.serviceWorker.register('./sw.js?v=12').catch(()=>{})}init();
