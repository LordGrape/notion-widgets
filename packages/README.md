# Shared packages

This folder contains shared code used across widgets:
- `packages/ui`: shared UI primitives, design tokens
- `packages/core`: shared runtime utilities
- `packages/utils`: small helpers

Keep package APIs small and stable so edits to one widget do not require reading the entire repo.
