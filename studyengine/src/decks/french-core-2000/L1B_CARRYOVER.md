# Phase L1b carry-over from L1a

L1a shipped: pure-TS pipeline, 50-lemma sample, dev-only import affordance,
gitignored corpus dumps with pinned-URL fetchers. L1b scales to 2000 + audio
+ shared-path edits. Items below were deferred from L1a explicitly.

## Shared-path edits (do these in one coherent phase)

- **Audio URL attachment** in `commitImport` and on the `CardJson` shape
  (currently no `audioUrl` field on `StudyItem`). Card chrome will need a
  play button — that's a §3.4 cohesion change with its own scoped phase.
- **CEFR sub-deck splitting** if useful: A1 (1–500) / A2 (501–1500) /
  B1 (1501–2000). May be sub-decks-of-sub-decks under "Core 2000".

## Card chrome (separate §3.4 cohesion phase)

- IPA slot (currently packed into `modelAnswer` markdown).
- Gender badge (currently packed into `modelAnswer` markdown).
- Audio play button (currently absent — no slot, no placeholder).
- Example sentence + translation slot (currently packed into `modelAnswer`
  blockquote).

## Content / data

- **LLM-generated glosses for full 2000 lemmas.** Replace
  `glosses-sample.json` with `studyengine/data/french-core-2000-glosses.json`.
  Worker route exists; auth header `X-Widget-Key: $WIDGET_SECRET` (see
  `worker/src/auth.ts`). Spot-check ~5% of generated glosses.
- **SAMPA → IPA mapping table.** Lexique 3 `phon` is SAMPA-style; needs
  conversion before any IPA slot can render the actual IPA glyphs. Build a
  TS lookup table in `studyengine/src/decks/french-core-2000/sampa-to-ipa.ts`.
- **Frequency-band tag scaling.** L1a tags everything `topic: 'A1'` (top 50).
  L1b: A1 for top 500, A2 for 501–1500, B1 for 1501–2000.
- **Tatoeba en-fr coverage check.** If coverage at full 2000 falls below
  ~80%, add an LLM-generated example fallback. Pipeline already handles
  `pair: undefined` gracefully (card emits without an example block).
- **Real lemmatisation** in `indexPairsByLemma` so verb forms like
  `vais` / `allé` get bucketed under `aller`. L1a uses surface-form
  matching only — fine for top-50 closed-class words but lossy at scale.

## Audio (L1b core)

- Forvo fetch + cache pipeline. Filter speakers by `userInfo.locale` where
  the API exposes it; prefer QC speakers per the §7.3 lock.
  License: CC BY-NC-SA 3.0 — attribution + non-commercial + share-alike.
- Google Cloud TTS `fr-CA` fallback for lemmas Forvo doesn't cover.

## Stretch / nice-to-have

- Replace `_meta._l1a_note` in `glosses-sample.json` with a proper schema
  versioning header once the file is regenerated by the LLM pipeline.
- Add a `verify-corpus-hashes.ts` script that re-checks SHA256s on demand
  (today they're checked only at fetch time).
