{
  "name": "studyengine-build",
  "private": true,
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.0.0",
    "typescript": "^5.6.3",
    "vitest": "^3.2.4"
  }
}
