{
  "name": "notion-widgets-worker",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5",
    "vitest": "^4.1.5",
    "wrangler": "^4"
  }
}
